# Spec Kit Bridge

## Purpose
This file explains how tku-update should use official GitHub Spec Kit alongside Codex Enhancer.

## Bridge Status
- Bridge mode: `attach`.
- Bridge state: `attached`.
- Bridge origin: `preexisting`.
- Official integration: `codex`.
- Script type: `ps`.
- Spec Kit version: `0.8.9.dev0`.
- Default command surface: $speckit-<command>.
- Official Spec Kit files were detected in this repo.

## Command Surface
- Use $speckit-<command> for official Spec Kit workflow steps.
- Expected commands in this repo: `constitution`, `specify`, `clarify`, `plan`, `analyze`, `tasks`, `implement`, `checklist`, `taskstoissues`, `git.commit`, `git.feature`, `git.initialize`, `git.remote`, `git.validate`.
- Use enhancer skills after Spec Kit has already produced artifacts; do not use the enhancer to replace official Spec Kit planning commands.

Use `python scripts/spec_kit_bridge.py` only as an imported helper for enhancer validation and installer flows. From a source or packaged enhancer checkout, prefer `codex-enhancer spec-report <repo>` for read-only feature artifact summaries, `codex-enhancer spec-sync <repo> --feature <feature> --changed <path>` for changed-path sync cues, and `codex-enhancer bridge <repo> --attach-spec-kit` for bridge-mode changes.

## Default Workflow
1. Start feature work with official Spec Kit: constitution -> specify -> clarify -> plan -> analyze -> tasks -> implement.
2. Before editing code, read the active `spec.md`, `plan.md`, `tasks.md`, and any `contracts/`, `quickstart.md`, `research.md`, or `data-model.md` files for that feature.
3. Implement the smallest coherent slice, then compare the code back against the feature artifacts.
4. Use the bridge skills below for implementation alignment, drift checks, and review prep.

## Bridge Skills
- `spec-implement-bridge`: use when a feature already has `plan.md` and `tasks.md` and you want repo-aware implementation work.
- `spec-sync-check`: use when code changed and you need to compare it against `spec.md`, tasks, contracts, quickstart notes, or other feature artifacts.
- `spec-review-bridge`: use when preparing a PR or review summary for a Spec Kit-driven branch.

## Ownership Boundary
- Treat official Spec Kit files as Spec Kit-owned: `.specify/`, `specs/`, and official generated prompt, agent, script, template, or skill files.
- Treat the managed Spec Kit bridge summary inside [AGENTS.md](../../AGENTS.md) as enhancer-owned and safe for future bridge refreshes.
- Treat this file and any installed bridge skills as enhancer-managed guidance that may be regenerated by later enhancer refreshes.
- Treat enhancer-owned bridge skills as `.codex/skills/` files only. If `.agents/skills/` exists, treat it as an external compatibility surface; do not mirror, migrate, or overwrite files there.

## Working Rule
- When a feature already has Spec Kit artifacts, read the active spec and plan before implementing code.
- Keep implementation, validation, and review notes aligned with the current feature artifacts instead of only the latest prompt.
- Do not use enhancer flows to rewrite official Spec Kit templates or feature history.
