# UE4 Asset Authoring API Stub Probe - 2026-05-10

- Generated from: `E:\Games\MechWarrior5Editor\MW5Mercs\Intermediate\PythonStub\unreal.py`
- Source condition: stub was generated by the launched MW5 Mod Editor GUI session on 2026-05-10.
- Safety: read-only stub inspection. No editor assets, mod files, paks, or modlist entries were changed.

## Relevant APIs Found

- `unreal.AssetTools.import_asset_tasks(import_tasks)`
- `unreal.AssetTools.import_assets_automated(import_data)`
- `unreal.AssetTools.duplicate_asset(asset_name, package_path, original_object)`
- `unreal.AssetImportTask` with `filename`, `destination_path`, `destination_name`, `factory`, `options`, `replace_existing`, `automated`, and `save`.
- `unreal.DataTableFactory` with writable `struct`.
- `unreal.CSVImportFactory` with writable `automated_import_settings`.
- `unreal.CSVImportSettings` with `import_row_struct` and `import_type`.
- `unreal.DataTableFunctionLibrary.fill_data_table_from_csv_file(data_table, csv_file_path)`.
- `unreal.EditorAssetLibrary.duplicate_asset(source_asset_path, destination_asset_path)`.
- `unreal.EditorAssetLibrary.save_asset(asset_to_save, only_if_is_dirty=True)`.

## Decision

After a proper MW5 editor mod target exists, Python likely can perform a controlled editor-side asset authoring test on the mod-owned copy of `/Game/InnerSphereData/MW5_InnerSphereData`:

1. Use the MW5 Mod Editor UI to create the mod target first.
2. Prefer the editor's `Save To Mod` UI for the initial current asset copy if available.
3. If a mod-owned DataTable copy exists, use `DataTableFunctionLibrary.fill_data_table_from_csv_file` with `reports\tku_editor_first\tku_inner_sphere_merged_current_plus_tku_additions_20260510.csv`.
4. Save only the mod-owned asset with `EditorAssetLibrary.save_asset`.
5. Verify row count and sample row IDs before any packaging.

This probe does not authorize direct modification of base `/Game` assets and does not authorize packaging. It only lowers the risk of a later mod-owned DataTable import once the editor-created target exists.
